<%= form_for(@dishtype) do |f| %>
  <% if @dishtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dishtype.errors.count, "error") %> prohibited this dishtype from being saved:</h2>

      <ul>
      <% @dishtype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
